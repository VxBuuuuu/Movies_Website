http://localhost:3000/
http://localhost:3000/admin/list
http://localhost:3000/admin/movie

bower.json is the dependency of the front-side（bower init）
package.json is the dependency of the back-side(npm init)